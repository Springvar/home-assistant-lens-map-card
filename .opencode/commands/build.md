---
description: Build the HACS card
agent: build
---

Build the lens-map-card for release:

```bash
npm run build
```

This outputs minified `dist/home-assistant-lens-map-card.js`. Run tests first if modifying code.